# Supabase — browser-safe (anon key only)
VITE_SUPABASE_URL=https://rsvswhrenusryblyzvcn.supabase.co
VITE_SUPABASE_ANON_KEY=isi_anon_key_dari_supabase_dashboard

# Jangan taruh service_role key atau secret lain dengan prefix VITE_ — itu akan terkirim ke browser.
# Server-only secrets (jika ada API route) taruh tanpa prefix VITE_ atau di dashboard Vercel sebagai Environment Variable (server).
